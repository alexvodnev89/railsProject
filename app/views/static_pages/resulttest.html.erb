<% provide(:title, "Result test page") %>
<p>Membership</p>

<div class="col-md-8">
    <% if @user %>
        <% if @user.microposts.any? %>
          <h3>Microposts (<%= @user.microposts.count %>)</h3>
        <% end %>
    <% end %>
  </div>